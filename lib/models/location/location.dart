class Location {
  final name;
  final shortName;
  final shortDescription;
  final description;
  final image;

  Location(this.name, this.shortName, this.shortDescription, this.description,
      this.image);
}
